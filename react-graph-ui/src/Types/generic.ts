export interface IXYPosition {
  x: number,
  y: number,
}

export type Dimension = {
  width: number,
  height: number
}

export interface ITransform {
  scale: number,
  translateX: number,
  translateY: number,
}